--- lib/presentation/screens/record_screen.dart
+++ lib/presentation/screens/record_screen.dart
@@ -6,6 +6,7 @@
 import '../../domain/models/tracking_metrics.dart';
 import '../providers/tracking_provider.dart';
 import '../widgets/route_map_widget.dart';
+import 'package:mapbox_maps_flutter/mapbox_maps_flutter.dart';
 import 'activity_detail_screen.dart';
 
 /// Record Screen - Strava-inspired GPS Activity Tracking
@@ -32,6 +33,7 @@
 
   bool _isLocked = false;
   int _currentMetricPage = 0;
+  String _mapStyleUri = MapboxStyles.DARK;
   final PageController _metricPageController = PageController();
 
   @override
@@ -217,6 +219,7 @@
             child: RouteMapWidget(
               routePoints: metrics.routePoints,
               isLiveTracking: true,
+              styleUri: _mapStyleUri,
               padding: const EdgeInsets.only(top: 80, bottom: 380, left: 40, right: 40),
             ),
           ),
@@ -279,6 +282,34 @@
                       ),
                     ),
                     const SizedBox(width: 8),
+                    // Map Style Toggle
+                    GestureDetector(
+                      onTap: () {
+                        HapticFeedback.lightImpact();
+                        setState(() {
+                          if (_mapStyleUri == MapboxStyles.DARK || _mapStyleUri == MapboxStyles.MAPBOX_STREETS) {
+                            _mapStyleUri = MapboxStyles.SATELLITE_STREETS;
+                          } else {
+                            _mapStyleUri = MapboxStyles.DARK; // Or MAPBOX_STREETS
+                          }
+                        });
+                      },
+                      child: Container(
+                        padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
+                        decoration: BoxDecoration(
+                          color: AppTheme.electricLime.withValues(alpha: 0.15),
+                          borderRadius: BorderRadius.circular(16),
+                          border: Border.all(color: AppTheme.electricLime.withValues(alpha: 0.5)),
+                        ),
+                        child: Text(
+                          _mapStyleUri == MapboxStyles.SATELLITE_STREETS ? 'Satellite' : 'Street',
+                          style: const TextStyle(
+                              color: AppTheme.electricLime, fontSize: 11, fontWeight: FontWeight.bold),
+                        ),
+                      ),
+                    ),
+                    const SizedBox(width: 8),
                     const _GpsSignalIndicator(compact: true),
                   ],
                 ),
