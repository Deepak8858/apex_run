org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# MAPBOX_DOWNLOADS_TOKEN must NOT be committed. Place it in:
#   ~/.gradle/gradle.properties        (user-global, recommended)
# OR set via CI secret + environment variable ORG_GRADLE_PROJECT_MAPBOX_DOWNLOADS_TOKEN
# See SECURITY_ROTATION.md.
